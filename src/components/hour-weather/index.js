/*jshint esversion:6*/
const template = require('./hour-weather.html');
const controller = require('./hour-weather.controller');

const HourWeatherComponent = {
  template,
  controller
};

module.exports = HourWeatherComponent;
